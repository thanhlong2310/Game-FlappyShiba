#ifndef CONSTANTS_H
#define CONSTANTS_H

const int SCREEN_WIDTH = 800;
const int SCREEN_HEIGHT = 600;
const int GRAVITY = 2;
const int JUMP_FORCE = -10;
const int PIPE_SPEED = 3;
const int PIPE_GAP = 200;
const int BOMB_SPEED = 4;      // Tốc độ bom
const int BOMB_SIZE = 30;      // Kích thước bom
const int BOMB_SPAWN_RATE = 150; // Tần suất tạo bom

#endif
